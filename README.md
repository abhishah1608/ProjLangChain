Demo on LLMChain - Integrated LLMChain with chatgpt API.
Integrated chains, promptemplate, parser (Json parser: get output in json format).
Here chains - Added SimpleSequentialChain, Sequential Chain, Conditional Chain.

Integrated webpage using streamlit library for the restaurant.

It will display list of restaurants, show the vegetarian menu dish.

Learned about prompt engineering - zero shot, few shot and chain of thought.

How efficient prompt can reduce input size of tokens and instead of retrieving the sentences one can get response in json
to reduce output tokens.









